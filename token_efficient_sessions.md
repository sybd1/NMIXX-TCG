# Antigravity 토큰 효율적인 세션 관리 가이드

Google Antigravity 환경에서 세션 토큰을 효과적으로 관리하는 것은 성능 최적화, 비용 절감, 그리고 엄격한 할당량 제한(쿼터 락아웃)을 방지하기 위해 매우 중요합니다. Antigravity는 에이전트 기반으로 작동하므로 사용자의 프롬프트뿐만 아니라 에이전트의 내부 추론(Thinking) 토큰, 코드 검색, 터미널 명령 실행, 서브에이전트 관리 과정에서도 토큰을 소비합니다.

이 가이드는 채팅 세션을 가볍고 빠르며 토큰 효율적으로 유지하기 위한 실용적인 전략을 설명합니다.

---

## 1. 세션 라이프사이클 관리

### 🔄 새로운 작업은 새 세션에서 시작하기
*   **문제점:** 대화가 길어질수록 전체 대화 기록이 매 메시지마다 컨텍스트로 함께 전송됩니다. 20단계가 넘어가는 긴 대화는 메시지당 수만 개의 토큰을 쉽게 소모하게 됩니다.
*   **해결책:** 서로 다른 작업이나 기능 개발을 시작할 때는 새로운 채팅 세션을 개설하세요. 특정 기능 구현 및 검증이 끝나면 기존 세션을 종료하고 새 대화를 시작하여 누적된 메시지 기록을 초기화하는 것이 좋습니다.

### 🎯 질문 범위 좁히기
*   에이전트에게 "처음부터 전체 기능을 다 구현해줘"라고 요청하는 대신, 먼저 아키텍처 설계를 요청하세요. 설계가 승인되면 파일 단위나 함수 단위로 나누어 코드 구현을 요청하는 것이 효율적입니다.

---

## 2. 작업 공간(Workspace) 최적화

### 📁 활성 컨텍스트 최소화
*   **`.gitignore` 및 제외 패턴 활용:** 빌드 산출물이나 대형 의존성 폴더(예: `node_modules`, `dist`, `build`, `.next`)가 `.gitignore`에 올바르게 등록되어 있는지 확인하세요. 에이전트의 파일 인덱싱 및 검색 도구가 불필요한 바이너리/라이브러리 파일을 탐색하며 토큰을 낭비하는 것을 방지합니다.
*   **대상 경로 지정 검색:** 검색을 요청할 때 가능하면 특정 하위 디렉터리(예: 루트 대신 `src/components`)를 지정하여 반환되는 검색 결과의 양을 제한하세요.

### 📝 지침 파일 설정 (`AGENTS.md` / `.geminirc`)
프로젝트 루트에 지침 파일을 추가하여 Antigravity 에이전트가 간결하게 답변하도록 규정할 수 있습니다. 예를 들어 다음과 같은 규칙을 추가하세요:
*   "답변은 최대한 간결하게 작성하고, 요청하지 않은 코드 설명은 생략하세요."
*   "변경되지 않은 전체 코드를 출력하지 말고, 변경된 부분(diff)만 정확히 제시하세요."
*   "불필요한 인사말이나 대화형 미사여구는 생략하세요."

---

## 3. 도구 및 모델 선택

### ⚡ 적절한 모델 라우팅
*   **Gemini Flash (Low):** 빠른 작업 수행, 파일 읽기, 간단한 스크립트 작성, 대량의 데이터 처리 작업에 최적입니다.
*   **Gemini Pro/Ultra (High):** 복잡한 리팩터링, 다중 파일 아키텍처 변경, 혹은 해결하기 까다로운 버그 디버깅에만 제한적으로 사용하세요.

### 🤖 서브에이전트 위임 활용
*   서브에이전트를 호출할 때(`invoke_subagent`), 작업 강도에 맞게 적절한 모델(예: 조사나 단순 파일 조회의 경우 `flash` 또는 `flash_lite`)을 지정하세요.
*   서브에이전트가 불필요하게 여러 레이어로 중첩되어 호출되는 것을 피하세요. 각 서브에이전트는 독립적인 대화 컨텍스트를 유지하므로 토큰 소모가 급증할 수 있습니다.

---

## 4. 토큰 낭비 방지 (무한 루프 차단)

### 🛑 비정상 루프 즉시 차단
에이전트가 무한 루프에 빠진 상황(예: 실패하는 테스트 명령을 반복 실행하거나, 유사한 키워드로 반복 검색을 수행하거나, 동일한 수정을 반복 시도할 때):
*   **즉시 중단:** 실행 중인 작업이나 명령을 취소(Cancel)하세요.
*   **직접 개입 및 교정:** 에이전트에게 루프가 발생하는 원인을 설명하거나 올바른 해결 방향을 메시지로 지시하세요.

### 📉 빌드 및 터미널 출력 필터링
*   수천 줄에 달하는 대용량 빌드 로그나 터미널 오류 로그를 그대로 대화창에 붙여넣지 마세요. 로그를 파일로 저장한 뒤 에이전트에게 특정 행 범위만 확인하게 하거나, 중요한 에러 부분만 잘라내어 전달하는 것이 좋습니다.

---

## 5. 핵심 요약 (Best Practices)

| 권장 행동 (Do) | 지양 행동 (Don't) |
| :--- | :--- |
| 각 작업마다 새로운 세션을 시작합니다. | 며칠 동안 단일 대화 세션을 길게 유지합니다. |
| 필요한 부분만 정확히 편집하도록 요청합니다. | 파일 전체를 다시 작성하라고 지시합니다. |
| 오류 로그 중 관련 있는 핵심 라인만 전달합니다. | 전체 빌드 로그나 시스템 덤프를 통째로 붙여넣습니다. |
| 지침 파일을 통해 에이전트의 답변을 간결하게 제어합니다. | 에이전트와 장황한 사담이나 부가 설명 대화를 나눕니다. |
